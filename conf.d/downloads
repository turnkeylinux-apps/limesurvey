#!/bin/bash -ex

dl() {
    [ "$FAB_HTTP_PROXY" ] && PROXY="--proxy $FAB_HTTP_PROXY"
    cd $2; curl -L -f -o limesurvey.tar.gz $PROXY $1; cd -
}


URL="https://www.limesurvey.org/stable-release?download=2404:limesurvey3121%20180616targz"
dl $URL /usr/local/src
