#!/bin/bash -ex

dl() {
    [ "$FAB_HTTP_PROXY" ] && PROXY="--proxy $FAB_HTTP_PROXY"
    cd $2; curl -L -f -O $PROXY $1; cd -
}

VERSION="limesurvey200plus-build130802.tar.gz"
URL="http://download.limesurvey.org/Latest_stable_release/$VERSION"

dl $URL /usr/local/src

